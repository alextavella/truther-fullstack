import { StyleSheet } from 'react-native'

export const s = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    gap: 8,
    paddingTop: 120,
    paddingHorizontal: 20,
  },
  footer: {
    marginTop: 16,
  },
})
